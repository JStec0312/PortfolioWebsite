import PageLoader from "./components/PageLoader";

export default function Loading() {
  return (
    <PageLoader>
      {/* This is a placeholder for the content that will be loaded */}
    </PageLoader>
  );
}